local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- DoorLock command GetLogRecord
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.DoorLock.GetLogRecord
--- @alias GetLogRecord
---
--- @field public ID number 0x04 the ID of this command
--- @field public NAME string "GetLogRecord" the name of this command
--- @field public log_index Uint16
local GetLogRecord = {}
GetLogRecord.NAME = "GetLogRecord"
GetLogRecord.ID = 0x04
GetLogRecord.args_def = {
  {
    name = "log_index",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function GetLogRecord:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetLogRecord.get_length = utils.length_from_fields
GetLogRecord.serialize = utils.serialize_from_fields
GetLogRecord.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetLogRecord
function GetLogRecord.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetLogRecord.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetLogRecord})
  out:set_field_names()
  return out
end

function GetLogRecord:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param log_index Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetLogRecord.build_test_rx(device, log_index)
  local out = {}
  local args = {log_index}
  for i,v in ipairs(GetLogRecord.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetLogRecord})
  out:set_field_names()
  return GetLogRecord._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GetLogRecord command
---
--- @param self GetLogRecord the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param log_index Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function GetLogRecord:init(device, log_index)
  local out = {}
  local args = {log_index}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetLogRecord,
    __tostring = GetLogRecord.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GetLogRecord:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetLogRecord, {__call = GetLogRecord.init})

return GetLogRecord
