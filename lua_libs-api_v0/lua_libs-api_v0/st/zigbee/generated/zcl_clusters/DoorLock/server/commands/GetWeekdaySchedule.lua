local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type DrlkTotalUserId
--- @alias DrlkTotalUserIdType
local DrlkTotalUserIdType = require "st.zigbee.generated.zcl_clusters.DoorLock.types.DrlkTotalUserId"
--- @type DrlkWeekDayScheduleId
--- @alias DrlkWeekDayScheduleIdType
local DrlkWeekDayScheduleIdType = require "st.zigbee.generated.zcl_clusters.DoorLock.types.DrlkWeekDayScheduleId"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- DoorLock command GetWeekdaySchedule
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.DoorLock.GetWeekdaySchedule
--- @alias GetWeekdaySchedule
---
--- @field public ID number 0x0C the ID of this command
--- @field public NAME string "GetWeekdaySchedule" the name of this command
--- @field public schedule_id st.zigbee.zcl.clusters.DoorLock.types.DrlkWeekDayScheduleId
--- @field public user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkTotalUserId
local GetWeekdaySchedule = {}
GetWeekdaySchedule.NAME = "GetWeekdaySchedule"
GetWeekdaySchedule.ID = 0x0C
GetWeekdaySchedule.args_def = {
  {
    name = "schedule_id",
    optional = false,
    data_type = DrlkWeekDayScheduleIdType,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "user_id",
    optional = false,
    data_type = DrlkTotalUserIdType,
    is_complex = false,
    default = 0x0000,
  },
}

function GetWeekdaySchedule:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetWeekdaySchedule.get_length = utils.length_from_fields
GetWeekdaySchedule.serialize = utils.serialize_from_fields
GetWeekdaySchedule.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetWeekdaySchedule
function GetWeekdaySchedule.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetWeekdaySchedule.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetWeekdaySchedule})
  out:set_field_names()
  return out
end

function GetWeekdaySchedule:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param schedule_id st.zigbee.zcl.clusters.DoorLock.types.DrlkWeekDayScheduleId
--- @param user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkTotalUserId
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetWeekdaySchedule.build_test_rx(device, schedule_id, user_id)
  local out = {}
  local args = {schedule_id, user_id}
  for i,v in ipairs(GetWeekdaySchedule.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetWeekdaySchedule})
  out:set_field_names()
  return GetWeekdaySchedule._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GetWeekdaySchedule command
---
--- @param self GetWeekdaySchedule the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param schedule_id st.zigbee.zcl.clusters.DoorLock.types.DrlkWeekDayScheduleId
--- @param user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkTotalUserId
--- @return ZigbeeMessageTx the full command addressed to the device
function GetWeekdaySchedule:init(device, schedule_id, user_id)
  local out = {}
  local args = {schedule_id, user_id}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetWeekdaySchedule,
    __tostring = GetWeekdaySchedule.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GetWeekdaySchedule:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetWeekdaySchedule, {__call = GetWeekdaySchedule.init})

return GetWeekdaySchedule
