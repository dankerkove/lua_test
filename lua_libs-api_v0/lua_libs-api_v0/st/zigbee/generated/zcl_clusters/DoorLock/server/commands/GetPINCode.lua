local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type DrlkPinUserId
--- @alias DrlkPinUserIdType
local DrlkPinUserIdType = require "st.zigbee.generated.zcl_clusters.DoorLock.types.DrlkPinUserId"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- DoorLock command GetPINCode
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.DoorLock.GetPINCode
--- @alias GetPINCode
---
--- @field public ID number 0x06 the ID of this command
--- @field public NAME string "GetPINCode" the name of this command
--- @field public user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkPinUserId
local GetPINCode = {}
GetPINCode.NAME = "GetPINCode"
GetPINCode.ID = 0x06
GetPINCode.args_def = {
  {
    name = "user_id",
    optional = false,
    data_type = DrlkPinUserIdType,
    is_complex = false,
    default = 0x0000,
  },
}

function GetPINCode:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetPINCode.get_length = utils.length_from_fields
GetPINCode.serialize = utils.serialize_from_fields
GetPINCode.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetPINCode
function GetPINCode.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetPINCode.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetPINCode})
  out:set_field_names()
  return out
end

function GetPINCode:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkPinUserId
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetPINCode.build_test_rx(device, user_id)
  local out = {}
  local args = {user_id}
  for i,v in ipairs(GetPINCode.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetPINCode})
  out:set_field_names()
  return GetPINCode._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GetPINCode command
---
--- @param self GetPINCode the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param user_id st.zigbee.zcl.clusters.DoorLock.types.DrlkPinUserId
--- @return ZigbeeMessageTx the full command addressed to the device
function GetPINCode:init(device, user_id)
  local out = {}
  local args = {user_id}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetPINCode,
    __tostring = GetPINCode.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GetPINCode:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetPINCode, {__call = GetPINCode.init})

return GetPINCode
