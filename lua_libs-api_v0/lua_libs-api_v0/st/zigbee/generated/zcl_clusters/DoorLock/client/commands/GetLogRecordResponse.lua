local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type DrlkOperationEventSource
--- @alias DrlkOperationEventSourceType
local DrlkOperationEventSourceType = require "st.zigbee.generated.zcl_clusters.DoorLock.types.DrlkOperationEventSource"
--- @type EventType
--- @alias EventTypeType
local EventTypeType = require "st.zigbee.generated.zcl_clusters.DoorLock.types.EventType"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- DoorLock command GetLogRecordResponse
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.DoorLock.GetLogRecordResponse
--- @alias GetLogRecordResponse
---
--- @field public ID number 0x04 the ID of this command
--- @field public NAME string "GetLogRecordResponse" the name of this command
--- @field public log_entry_id Uint16
--- @field public timestamp Uint32
--- @field public event_type st.zigbee.zcl.clusters.DoorLock.EventType
--- @field public source_operation_event st.zigbee.zcl.clusters.DoorLock.types.DrlkOperationEventSource
--- @field public event_id_or_alarm_code Uint8
--- @field public user_id Uint16
--- @field public pin OctetString
local GetLogRecordResponse = {}
GetLogRecordResponse.NAME = "GetLogRecordResponse"
GetLogRecordResponse.ID = 0x04
GetLogRecordResponse.args_def = {
  {
    name = "log_entry_id",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "timestamp",
    optional = false,
    data_type = data_types.Uint32,
    is_complex = false,
    default = 0x00000000,
  },
  {
    name = "event_type",
    optional = false,
    data_type = EventTypeType,
    is_complex = false,
  },
  {
    name = "source_operation_event",
    optional = false,
    data_type = DrlkOperationEventSourceType,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "event_id_or_alarm_code",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "user_id",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "pin",
    optional = false,
    data_type = data_types.OctetString,
    is_complex = false,
    default = "",
  },
}

function GetLogRecordResponse:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetLogRecordResponse.get_length = utils.length_from_fields
GetLogRecordResponse.serialize = utils.serialize_from_fields
GetLogRecordResponse.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetLogRecordResponse
function GetLogRecordResponse.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetLogRecordResponse.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetLogRecordResponse})
  out:set_field_names()
  return out
end

function GetLogRecordResponse:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param log_entry_id Uint16
--- @param timestamp Uint32
--- @param event_type st.zigbee.zcl.clusters.DoorLock.EventType
--- @param source_operation_event st.zigbee.zcl.clusters.DoorLock.types.DrlkOperationEventSource
--- @param event_id_or_alarm_code Uint8
--- @param user_id Uint16
--- @param pin OctetString
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetLogRecordResponse.build_test_rx(device, log_entry_id, timestamp, event_type, source_operation_event, event_id_or_alarm_code, user_id, pin)
  local out = {}
  local args = {log_entry_id, timestamp, event_type, source_operation_event, event_id_or_alarm_code, user_id, pin}
  for i,v in ipairs(GetLogRecordResponse.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetLogRecordResponse})
  out:set_field_names()
  return GetLogRecordResponse._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the GetLogRecordResponse command
---
--- @param self GetLogRecordResponse the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param log_entry_id Uint16
--- @param timestamp Uint32
--- @param event_type st.zigbee.zcl.clusters.DoorLock.EventType
--- @param source_operation_event st.zigbee.zcl.clusters.DoorLock.types.DrlkOperationEventSource
--- @param event_id_or_alarm_code Uint8
--- @param user_id Uint16
--- @param pin OctetString
--- @return ZigbeeMessageTx the full command addressed to the device
function GetLogRecordResponse:init(device, log_entry_id, timestamp, event_type, source_operation_event, event_id_or_alarm_code, user_id, pin)
  local out = {}
  local args = {log_entry_id, timestamp, event_type, source_operation_event, event_id_or_alarm_code, user_id, pin}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetLogRecordResponse,
    __tostring = GetLogRecordResponse.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function GetLogRecordResponse:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetLogRecordResponse, {__call = GetLogRecordResponse.init})

return GetLogRecordResponse
