local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASACE command Bypass
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASACE.Bypass
--- @alias Bypass
---
--- @field public ID number 0x01 the ID of this command
--- @field public NAME string "Bypass" the name of this command
--- @field public zone_id Uint8
--- @field public arm_disarm_code CharString
local Bypass = {}
Bypass.NAME = "Bypass"
Bypass.ID = 0x01
Bypass.args_def = {
  {
    name = "zone_id",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "arm_disarm_code",
    optional = false,
    data_type = data_types.CharString,
    is_complex = false,
    default = "",
  },
}

function Bypass:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

Bypass.get_length = utils.length_from_fields
Bypass.serialize = utils.serialize_from_fields
Bypass.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return Bypass
function Bypass.deserialize(buf)
  local out = {}
  for _, v in ipairs(Bypass.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = Bypass})
  out:set_field_names()
  return out
end

function Bypass:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param zone_id Uint8
--- @param arm_disarm_code CharString
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function Bypass.build_test_rx(device, zone_id, arm_disarm_code)
  local out = {}
  local args = {zone_id, arm_disarm_code}
  for i,v in ipairs(Bypass.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = Bypass})
  out:set_field_names()
  return Bypass._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the Bypass command
---
--- @param self Bypass the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param zone_id Uint8
--- @param arm_disarm_code CharString
--- @return ZigbeeMessageTx the full command addressed to the device
function Bypass:init(device, zone_id, arm_disarm_code)
  local out = {}
  local args = {zone_id, arm_disarm_code}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = Bypass,
    __tostring = Bypass.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function Bypass:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(Bypass, {__call = Bypass.init})

return Bypass
