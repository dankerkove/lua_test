local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type IasaceAudibleNotification
--- @alias IasaceAudibleNotificationType
local IasaceAudibleNotificationType = require "st.zigbee.generated.zcl_clusters.IASACE.types.IasaceAudibleNotification"
--- @type IasacePanelStatus
--- @alias IasacePanelStatusType
local IasacePanelStatusType = require "st.zigbee.generated.zcl_clusters.IASACE.types.IasacePanelStatus"
--- @type IasaceAlarmStatus
--- @alias IasaceAlarmStatusType
local IasaceAlarmStatusType = require "st.zigbee.generated.zcl_clusters.IASACE.types.IasaceAlarmStatus"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASACE command PanelStatusChanged
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASACE.PanelStatusChanged
--- @alias PanelStatusChanged
---
--- @field public ID number 0x04 the ID of this command
--- @field public NAME string "PanelStatusChanged" the name of this command
--- @field public panel_status st.zigbee.zcl.clusters.IASACE.types.IasacePanelStatus
--- @field public seconds_remaining Uint8
--- @field public audible_notification st.zigbee.zcl.clusters.IASACE.types.IasaceAudibleNotification
--- @field public alarm_status st.zigbee.zcl.clusters.IASACE.types.IasaceAlarmStatus
local PanelStatusChanged = {}
PanelStatusChanged.NAME = "PanelStatusChanged"
PanelStatusChanged.ID = 0x04
PanelStatusChanged.args_def = {
  {
    name = "panel_status",
    optional = false,
    data_type = IasacePanelStatusType,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "seconds_remaining",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "audible_notification",
    optional = false,
    data_type = IasaceAudibleNotificationType,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "alarm_status",
    optional = false,
    data_type = IasaceAlarmStatusType,
    is_complex = false,
    default = 0x00,
  },
}

function PanelStatusChanged:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

PanelStatusChanged.get_length = utils.length_from_fields
PanelStatusChanged.serialize = utils.serialize_from_fields
PanelStatusChanged.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return PanelStatusChanged
function PanelStatusChanged.deserialize(buf)
  local out = {}
  for _, v in ipairs(PanelStatusChanged.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = PanelStatusChanged})
  out:set_field_names()
  return out
end

function PanelStatusChanged:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param panel_status st.zigbee.zcl.clusters.IASACE.types.IasacePanelStatus
--- @param seconds_remaining Uint8
--- @param audible_notification st.zigbee.zcl.clusters.IASACE.types.IasaceAudibleNotification
--- @param alarm_status st.zigbee.zcl.clusters.IASACE.types.IasaceAlarmStatus
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function PanelStatusChanged.build_test_rx(device, panel_status, seconds_remaining, audible_notification, alarm_status)
  local out = {}
  local args = {panel_status, seconds_remaining, audible_notification, alarm_status}
  for i,v in ipairs(PanelStatusChanged.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = PanelStatusChanged})
  out:set_field_names()
  return PanelStatusChanged._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the PanelStatusChanged command
---
--- @param self PanelStatusChanged the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param panel_status st.zigbee.zcl.clusters.IASACE.types.IasacePanelStatus
--- @param seconds_remaining Uint8
--- @param audible_notification st.zigbee.zcl.clusters.IASACE.types.IasaceAudibleNotification
--- @param alarm_status st.zigbee.zcl.clusters.IASACE.types.IasaceAlarmStatus
--- @return ZigbeeMessageTx the full command addressed to the device
function PanelStatusChanged:init(device, panel_status, seconds_remaining, audible_notification, alarm_status)
  local out = {}
  local args = {panel_status, seconds_remaining, audible_notification, alarm_status}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = PanelStatusChanged,
    __tostring = PanelStatusChanged.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function PanelStatusChanged:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(PanelStatusChanged, {__call = PanelStatusChanged.init})

return PanelStatusChanged
