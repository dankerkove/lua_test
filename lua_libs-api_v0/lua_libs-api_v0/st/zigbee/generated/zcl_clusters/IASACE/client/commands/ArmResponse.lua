local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type ArmNotification
--- @alias ArmNotificationType
local ArmNotificationType = require "st.zigbee.generated.zcl_clusters.IASACE.types.ArmNotification"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASACE command ArmResponse
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASACE.ArmResponse
--- @alias ArmResponse
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "ArmResponse" the name of this command
--- @field public arm_notification st.zigbee.zcl.clusters.IASACE.ArmNotification
local ArmResponse = {}
ArmResponse.NAME = "ArmResponse"
ArmResponse.ID = 0x00
ArmResponse.args_def = {
  {
    name = "arm_notification",
    optional = false,
    data_type = ArmNotificationType,
    is_complex = false,
  },
}

function ArmResponse:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

ArmResponse.get_length = utils.length_from_fields
ArmResponse.serialize = utils.serialize_from_fields
ArmResponse.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return ArmResponse
function ArmResponse.deserialize(buf)
  local out = {}
  for _, v in ipairs(ArmResponse.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = ArmResponse})
  out:set_field_names()
  return out
end

function ArmResponse:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param arm_notification st.zigbee.zcl.clusters.IASACE.ArmNotification
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function ArmResponse.build_test_rx(device, arm_notification)
  local out = {}
  local args = {arm_notification}
  for i,v in ipairs(ArmResponse.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = ArmResponse})
  out:set_field_names()
  return ArmResponse._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the ArmResponse command
---
--- @param self ArmResponse the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param arm_notification st.zigbee.zcl.clusters.IASACE.ArmNotification
--- @return ZigbeeMessageTx the full command addressed to the device
function ArmResponse:init(device, arm_notification)
  local out = {}
  local args = {arm_notification}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = ArmResponse,
    __tostring = ArmResponse.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function ArmResponse:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(ArmResponse, {__call = ArmResponse.init})

return ArmResponse
