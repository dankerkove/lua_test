local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type ZoneIdBypassResult
--- @alias ZoneIdBypassResultType
local ZoneIdBypassResultType = require "st.zigbee.generated.zcl_clusters.IASACE.types.ZoneIdBypassResult"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASACE command BypassResponse
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASACE.BypassResponse
--- @alias BypassResponse
---
--- @field public ID number 0x07 the ID of this command
--- @field public NAME string "BypassResponse" the name of this command
--- @field public zone_id_bypass_result st.zigbee.zcl.clusters.IASACE.ZoneIdBypassResult
local BypassResponse = {}
BypassResponse.NAME = "BypassResponse"
BypassResponse.ID = 0x07
BypassResponse.args_def = {
  {
    name = "zone_id_bypass_result",
    optional = false,
    data_type = ZoneIdBypassResultType,
    is_complex = false,
  },
}

function BypassResponse:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

BypassResponse.get_length = utils.length_from_fields
BypassResponse.serialize = utils.serialize_from_fields
BypassResponse.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return BypassResponse
function BypassResponse.deserialize(buf)
  local out = {}
  for _, v in ipairs(BypassResponse.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = BypassResponse})
  out:set_field_names()
  return out
end

function BypassResponse:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param zone_id_bypass_result st.zigbee.zcl.clusters.IASACE.ZoneIdBypassResult
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function BypassResponse.build_test_rx(device, zone_id_bypass_result)
  local out = {}
  local args = {zone_id_bypass_result}
  for i,v in ipairs(BypassResponse.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = BypassResponse})
  out:set_field_names()
  return BypassResponse._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the BypassResponse command
---
--- @param self BypassResponse the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param zone_id_bypass_result st.zigbee.zcl.clusters.IASACE.ZoneIdBypassResult
--- @return ZigbeeMessageTx the full command addressed to the device
function BypassResponse:init(device, zone_id_bypass_result)
  local out = {}
  local args = {zone_id_bypass_result}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = BypassResponse,
    __tostring = BypassResponse.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function BypassResponse:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(BypassResponse, {__call = BypassResponse.init})

return BypassResponse
