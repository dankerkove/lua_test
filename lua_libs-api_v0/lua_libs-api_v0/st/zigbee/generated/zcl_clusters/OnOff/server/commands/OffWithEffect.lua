local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type EffectIdentifier
--- @alias EffectIdentifierType
local EffectIdentifierType = require "st.zigbee.generated.zcl_clusters.OnOff.types.EffectIdentifier"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- OnOff command OffWithEffect
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.OnOff.OffWithEffect
--- @alias OffWithEffect
---
--- @field public ID number 0x40 the ID of this command
--- @field public NAME string "OffWithEffect" the name of this command
--- @field public effect_identifier st.zigbee.zcl.clusters.OnOff.EffectIdentifier
--- @field public effect_variant Uint8
local OffWithEffect = {}
OffWithEffect.NAME = "OffWithEffect"
OffWithEffect.ID = 0x40
OffWithEffect.args_def = {
  {
    name = "effect_identifier",
    optional = false,
    data_type = EffectIdentifierType,
    is_complex = false,
  },
  {
    name = "effect_variant",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function OffWithEffect:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

OffWithEffect.get_length = utils.length_from_fields
OffWithEffect.serialize = utils.serialize_from_fields
OffWithEffect.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return OffWithEffect
function OffWithEffect.deserialize(buf)
  local out = {}
  for _, v in ipairs(OffWithEffect.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = OffWithEffect})
  out:set_field_names()
  return out
end

function OffWithEffect:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param effect_identifier st.zigbee.zcl.clusters.OnOff.EffectIdentifier
--- @param effect_variant Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function OffWithEffect.build_test_rx(device, effect_identifier, effect_variant)
  local out = {}
  local args = {effect_identifier, effect_variant}
  for i,v in ipairs(OffWithEffect.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = OffWithEffect})
  out:set_field_names()
  return OffWithEffect._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the OffWithEffect command
---
--- @param self OffWithEffect the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param effect_identifier st.zigbee.zcl.clusters.OnOff.EffectIdentifier
--- @param effect_variant Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function OffWithEffect:init(device, effect_identifier, effect_variant)
  local out = {}
  local args = {effect_identifier, effect_variant}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = OffWithEffect,
    __tostring = OffWithEffect.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function OffWithEffect:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(OffWithEffect, {__call = OffWithEffect.init})

return OffWithEffect
