local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type OnOffControl
--- @alias OnOffControlType
local OnOffControlType = require "st.zigbee.generated.zcl_clusters.OnOff.types.OnOffControl"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- OnOff command OnWithTimedOff
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.OnOff.OnWithTimedOff
--- @alias OnWithTimedOff
---
--- @field public ID number 0x42 the ID of this command
--- @field public NAME string "OnWithTimedOff" the name of this command
--- @field public on_off_control st.zigbee.zcl.clusters.OnOff.OnOffControl
--- @field public on_time Uint16
--- @field public off_wait_time Uint16
local OnWithTimedOff = {}
OnWithTimedOff.NAME = "OnWithTimedOff"
OnWithTimedOff.ID = 0x42
OnWithTimedOff.args_def = {
  {
    name = "on_off_control",
    optional = false,
    data_type = OnOffControlType,
    is_complex = false,
  },
  {
    name = "on_time",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "off_wait_time",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function OnWithTimedOff:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

OnWithTimedOff.get_length = utils.length_from_fields
OnWithTimedOff.serialize = utils.serialize_from_fields
OnWithTimedOff.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return OnWithTimedOff
function OnWithTimedOff.deserialize(buf)
  local out = {}
  for _, v in ipairs(OnWithTimedOff.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = OnWithTimedOff})
  out:set_field_names()
  return out
end

function OnWithTimedOff:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param on_off_control st.zigbee.zcl.clusters.OnOff.OnOffControl
--- @param on_time Uint16
--- @param off_wait_time Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function OnWithTimedOff.build_test_rx(device, on_off_control, on_time, off_wait_time)
  local out = {}
  local args = {on_off_control, on_time, off_wait_time}
  for i,v in ipairs(OnWithTimedOff.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = OnWithTimedOff})
  out:set_field_names()
  return OnWithTimedOff._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the OnWithTimedOff command
---
--- @param self OnWithTimedOff the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param on_off_control st.zigbee.zcl.clusters.OnOff.OnOffControl
--- @param on_time Uint16
--- @param off_wait_time Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function OnWithTimedOff:init(device, on_off_control, on_time, off_wait_time)
  local out = {}
  local args = {on_off_control, on_time, off_wait_time}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = OnWithTimedOff,
    __tostring = OnWithTimedOff.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function OnWithTimedOff:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(OnWithTimedOff, {__call = OnWithTimedOff.init})

return OnWithTimedOff
