local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- WindowCovering command GoToTiltValue
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.WindowCovering.GoToTiltValue
--- @alias GoToTiltValue
---
--- @field public ID number 0x07 the ID of this command
--- @field public NAME string "GoToTiltValue" the name of this command
--- @field public tilt_value Uint16
local GoToTiltValue = {}
GoToTiltValue.NAME = "GoToTiltValue"
GoToTiltValue.ID = 0x07
GoToTiltValue.args_def = {
  {
    name = "tilt_value",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function GoToTiltValue:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GoToTiltValue.get_length = utils.length_from_fields
GoToTiltValue.serialize = utils.serialize_from_fields
GoToTiltValue.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GoToTiltValue
function GoToTiltValue.deserialize(buf)
  local out = {}
  for _, v in ipairs(GoToTiltValue.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GoToTiltValue})
  out:set_field_names()
  return out
end

function GoToTiltValue:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param tilt_value Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GoToTiltValue.build_test_rx(device, tilt_value)
  local out = {}
  local args = {tilt_value}
  for i,v in ipairs(GoToTiltValue.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GoToTiltValue})
  out:set_field_names()
  return GoToTiltValue._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GoToTiltValue command
---
--- @param self GoToTiltValue the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param tilt_value Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function GoToTiltValue:init(device, tilt_value)
  local out = {}
  local args = {tilt_value}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GoToTiltValue,
    __tostring = GoToTiltValue.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GoToTiltValue:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GoToTiltValue, {__call = GoToTiltValue.init})

return GoToTiltValue
