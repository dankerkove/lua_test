local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- WindowCovering command GoToLiftPercentage
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.WindowCovering.GoToLiftPercentage
--- @alias GoToLiftPercentage
---
--- @field public ID number 0x05 the ID of this command
--- @field public NAME string "GoToLiftPercentage" the name of this command
--- @field public percentage_lift_value Uint8
local GoToLiftPercentage = {}
GoToLiftPercentage.NAME = "GoToLiftPercentage"
GoToLiftPercentage.ID = 0x05
GoToLiftPercentage.args_def = {
  {
    name = "percentage_lift_value",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function GoToLiftPercentage:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GoToLiftPercentage.get_length = utils.length_from_fields
GoToLiftPercentage.serialize = utils.serialize_from_fields
GoToLiftPercentage.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GoToLiftPercentage
function GoToLiftPercentage.deserialize(buf)
  local out = {}
  for _, v in ipairs(GoToLiftPercentage.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GoToLiftPercentage})
  out:set_field_names()
  return out
end

function GoToLiftPercentage:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param percentage_lift_value Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GoToLiftPercentage.build_test_rx(device, percentage_lift_value)
  local out = {}
  local args = {percentage_lift_value}
  for i,v in ipairs(GoToLiftPercentage.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GoToLiftPercentage})
  out:set_field_names()
  return GoToLiftPercentage._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GoToLiftPercentage command
---
--- @param self GoToLiftPercentage the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param percentage_lift_value Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function GoToLiftPercentage:init(device, percentage_lift_value)
  local out = {}
  local args = {percentage_lift_value}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GoToLiftPercentage,
    __tostring = GoToLiftPercentage.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GoToLiftPercentage:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GoToLiftPercentage, {__call = GoToLiftPercentage.init})

return GoToLiftPercentage
