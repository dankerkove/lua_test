local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type EnrollResponseCode
--- @alias EnrollResponseCodeType
local EnrollResponseCodeType = require "st.zigbee.generated.zcl_clusters.IASZone.types.EnrollResponseCode"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASZone command ZoneEnrollResponse
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASZone.ZoneEnrollResponse
--- @alias ZoneEnrollResponse
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "ZoneEnrollResponse" the name of this command
--- @field public enroll_response_code st.zigbee.zcl.clusters.IASZone.EnrollResponseCode
--- @field public zone_id Uint8
local ZoneEnrollResponse = {}
ZoneEnrollResponse.NAME = "ZoneEnrollResponse"
ZoneEnrollResponse.ID = 0x00
ZoneEnrollResponse.args_def = {
  {
    name = "enroll_response_code",
    optional = false,
    data_type = EnrollResponseCodeType,
    is_complex = false,
  },
  {
    name = "zone_id",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function ZoneEnrollResponse:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

ZoneEnrollResponse.get_length = utils.length_from_fields
ZoneEnrollResponse.serialize = utils.serialize_from_fields
ZoneEnrollResponse.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return ZoneEnrollResponse
function ZoneEnrollResponse.deserialize(buf)
  local out = {}
  for _, v in ipairs(ZoneEnrollResponse.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = ZoneEnrollResponse})
  out:set_field_names()
  return out
end

function ZoneEnrollResponse:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param enroll_response_code st.zigbee.zcl.clusters.IASZone.EnrollResponseCode
--- @param zone_id Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function ZoneEnrollResponse.build_test_rx(device, enroll_response_code, zone_id)
  local out = {}
  local args = {enroll_response_code, zone_id}
  for i,v in ipairs(ZoneEnrollResponse.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = ZoneEnrollResponse})
  out:set_field_names()
  return ZoneEnrollResponse._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the ZoneEnrollResponse command
---
--- @param self ZoneEnrollResponse the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param enroll_response_code st.zigbee.zcl.clusters.IASZone.EnrollResponseCode
--- @param zone_id Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function ZoneEnrollResponse:init(device, enroll_response_code, zone_id)
  local out = {}
  local args = {enroll_response_code, zone_id}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = ZoneEnrollResponse,
    __tostring = ZoneEnrollResponse.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function ZoneEnrollResponse:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(ZoneEnrollResponse, {__call = ZoneEnrollResponse.init})

return ZoneEnrollResponse
