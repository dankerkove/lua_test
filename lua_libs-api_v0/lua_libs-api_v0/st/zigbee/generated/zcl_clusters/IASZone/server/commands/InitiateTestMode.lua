local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASZone command InitiateTestMode
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASZone.InitiateTestMode
--- @alias InitiateTestMode
---
--- @field public ID number 0x02 the ID of this command
--- @field public NAME string "InitiateTestMode" the name of this command
--- @field public test_mode_duration Uint8
--- @field public current_zone_sensitivity_level Uint8
local InitiateTestMode = {}
InitiateTestMode.NAME = "InitiateTestMode"
InitiateTestMode.ID = 0x02
InitiateTestMode.args_def = {
  {
    name = "test_mode_duration",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "current_zone_sensitivity_level",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function InitiateTestMode:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

InitiateTestMode.get_length = utils.length_from_fields
InitiateTestMode.serialize = utils.serialize_from_fields
InitiateTestMode.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return InitiateTestMode
function InitiateTestMode.deserialize(buf)
  local out = {}
  for _, v in ipairs(InitiateTestMode.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = InitiateTestMode})
  out:set_field_names()
  return out
end

function InitiateTestMode:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param test_mode_duration Uint8
--- @param current_zone_sensitivity_level Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function InitiateTestMode.build_test_rx(device, test_mode_duration, current_zone_sensitivity_level)
  local out = {}
  local args = {test_mode_duration, current_zone_sensitivity_level}
  for i,v in ipairs(InitiateTestMode.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = InitiateTestMode})
  out:set_field_names()
  return InitiateTestMode._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the InitiateTestMode command
---
--- @param self InitiateTestMode the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param test_mode_duration Uint8
--- @param current_zone_sensitivity_level Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function InitiateTestMode:init(device, test_mode_duration, current_zone_sensitivity_level)
  local out = {}
  local args = {test_mode_duration, current_zone_sensitivity_level}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = InitiateTestMode,
    __tostring = InitiateTestMode.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function InitiateTestMode:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(InitiateTestMode, {__call = InitiateTestMode.init})

return InitiateTestMode
