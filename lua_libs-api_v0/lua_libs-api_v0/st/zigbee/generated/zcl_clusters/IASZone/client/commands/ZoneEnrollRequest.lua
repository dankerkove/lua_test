local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
local IasZoneType = require "st.zigbee.generated.types.IasZoneType"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASZone command ZoneEnrollRequest
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASZone.ZoneEnrollRequest
--- @alias ZoneEnrollRequest
---
--- @field public ID number 0x01 the ID of this command
--- @field public NAME string "ZoneEnrollRequest" the name of this command
--- @field public zone_type IasZoneType
--- @field public manufacturer_code Uint16
local ZoneEnrollRequest = {}
ZoneEnrollRequest.NAME = "ZoneEnrollRequest"
ZoneEnrollRequest.ID = 0x01
ZoneEnrollRequest.args_def = {
  {
    name = "zone_type",
    optional = false,
    data_type = IasZoneType,
    is_complex = false,
  },
  {
    name = "manufacturer_code",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function ZoneEnrollRequest:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

ZoneEnrollRequest.get_length = utils.length_from_fields
ZoneEnrollRequest.serialize = utils.serialize_from_fields
ZoneEnrollRequest.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return ZoneEnrollRequest
function ZoneEnrollRequest.deserialize(buf)
  local out = {}
  for _, v in ipairs(ZoneEnrollRequest.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = ZoneEnrollRequest})
  out:set_field_names()
  return out
end

function ZoneEnrollRequest:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param zone_type IasZoneType
--- @param manufacturer_code Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function ZoneEnrollRequest.build_test_rx(device, zone_type, manufacturer_code)
  local out = {}
  local args = {zone_type, manufacturer_code}
  for i,v in ipairs(ZoneEnrollRequest.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = ZoneEnrollRequest})
  out:set_field_names()
  return ZoneEnrollRequest._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the ZoneEnrollRequest command
---
--- @param self ZoneEnrollRequest the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param zone_type IasZoneType
--- @param manufacturer_code Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function ZoneEnrollRequest:init(device, zone_type, manufacturer_code)
  local out = {}
  local args = {zone_type, manufacturer_code}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = ZoneEnrollRequest,
    __tostring = ZoneEnrollRequest.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function ZoneEnrollRequest:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(ZoneEnrollRequest, {__call = ZoneEnrollRequest.init})

return ZoneEnrollRequest
