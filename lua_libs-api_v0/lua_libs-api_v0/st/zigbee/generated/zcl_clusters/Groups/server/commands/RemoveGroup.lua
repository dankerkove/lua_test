local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Groups command RemoveGroup
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Groups.RemoveGroup
--- @alias RemoveGroup
---
--- @field public ID number 0x03 the ID of this command
--- @field public NAME string "RemoveGroup" the name of this command
--- @field public group_id Uint16
local RemoveGroup = {}
RemoveGroup.NAME = "RemoveGroup"
RemoveGroup.ID = 0x03
RemoveGroup.args_def = {
  {
    name = "group_id",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function RemoveGroup:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

RemoveGroup.get_length = utils.length_from_fields
RemoveGroup.serialize = utils.serialize_from_fields
RemoveGroup.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return RemoveGroup
function RemoveGroup.deserialize(buf)
  local out = {}
  for _, v in ipairs(RemoveGroup.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = RemoveGroup})
  out:set_field_names()
  return out
end

function RemoveGroup:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param group_id Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function RemoveGroup.build_test_rx(device, group_id)
  local out = {}
  local args = {group_id}
  for i,v in ipairs(RemoveGroup.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = RemoveGroup})
  out:set_field_names()
  return RemoveGroup._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the RemoveGroup command
---
--- @param self RemoveGroup the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param group_id Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function RemoveGroup:init(device, group_id)
  local out = {}
  local args = {group_id}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = RemoveGroup,
    __tostring = RemoveGroup.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function RemoveGroup:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(RemoveGroup, {__call = RemoveGroup.init})

return RemoveGroup
