local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Groups command AddGroupIfIdentifying
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Groups.AddGroupIfIdentifying
--- @alias AddGroupIfIdentifying
---
--- @field public ID number 0x05 the ID of this command
--- @field public NAME string "AddGroupIfIdentifying" the name of this command
--- @field public group_id Uint16
--- @field public group_name CharString
local AddGroupIfIdentifying = {}
AddGroupIfIdentifying.NAME = "AddGroupIfIdentifying"
AddGroupIfIdentifying.ID = 0x05
AddGroupIfIdentifying.args_def = {
  {
    name = "group_id",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "group_name",
    optional = false,
    data_type = data_types.CharString,
    is_complex = false,
    default = "",
  },
}

function AddGroupIfIdentifying:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

AddGroupIfIdentifying.get_length = utils.length_from_fields
AddGroupIfIdentifying.serialize = utils.serialize_from_fields
AddGroupIfIdentifying.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return AddGroupIfIdentifying
function AddGroupIfIdentifying.deserialize(buf)
  local out = {}
  for _, v in ipairs(AddGroupIfIdentifying.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = AddGroupIfIdentifying})
  out:set_field_names()
  return out
end

function AddGroupIfIdentifying:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param group_id Uint16
--- @param group_name CharString
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function AddGroupIfIdentifying.build_test_rx(device, group_id, group_name)
  local out = {}
  local args = {group_id, group_name}
  for i,v in ipairs(AddGroupIfIdentifying.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = AddGroupIfIdentifying})
  out:set_field_names()
  return AddGroupIfIdentifying._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the AddGroupIfIdentifying command
---
--- @param self AddGroupIfIdentifying the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param group_id Uint16
--- @param group_name CharString
--- @return ZigbeeMessageTx the full command addressed to the device
function AddGroupIfIdentifying:init(device, group_id, group_name)
  local out = {}
  local args = {group_id, group_name}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = AddGroupIfIdentifying,
    __tostring = AddGroupIfIdentifying.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function AddGroupIfIdentifying:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(AddGroupIfIdentifying, {__call = AddGroupIfIdentifying.init})

return AddGroupIfIdentifying
