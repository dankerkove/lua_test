local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Groups command GetGroupMembership
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Groups.GetGroupMembership
--- @alias GetGroupMembership
---
--- @field public ID number 0x02 the ID of this command
--- @field public NAME string "GetGroupMembership" the name of this command
--- @field public group_list Uint16
local GetGroupMembership = {}
GetGroupMembership.NAME = "GetGroupMembership"
GetGroupMembership.ID = 0x02
GetGroupMembership.args_def = {
  {
    name = "group_list",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
}

function GetGroupMembership:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetGroupMembership.get_length = utils.length_from_fields
GetGroupMembership.serialize = utils.serialize_from_fields
GetGroupMembership.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetGroupMembership
function GetGroupMembership.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetGroupMembership.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetGroupMembership})
  out:set_field_names()
  return out
end

function GetGroupMembership:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param group_list Uint16
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetGroupMembership.build_test_rx(device, group_list)
  local out = {}
  local args = {group_list}
  for i,v in ipairs(GetGroupMembership.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetGroupMembership})
  out:set_field_names()
  return GetGroupMembership._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GetGroupMembership command
---
--- @param self GetGroupMembership the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param group_list Uint16
--- @return ZigbeeMessageTx the full command addressed to the device
function GetGroupMembership:init(device, group_list)
  local out = {}
  local args = {group_list}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetGroupMembership,
    __tostring = GetGroupMembership.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GetGroupMembership:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetGroupMembership, {__call = GetGroupMembership.init})

return GetGroupMembership
