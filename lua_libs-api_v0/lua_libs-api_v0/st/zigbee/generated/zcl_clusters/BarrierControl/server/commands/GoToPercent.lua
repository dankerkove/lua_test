local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- BarrierControl command GoToPercent
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.BarrierControl.GoToPercent
--- @alias GoToPercent
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "GoToPercent" the name of this command
--- @field public percent_open Uint8
local GoToPercent = {}
GoToPercent.NAME = "GoToPercent"
GoToPercent.ID = 0x00
GoToPercent.args_def = {
  {
    name = "percent_open",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function GoToPercent:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GoToPercent.get_length = utils.length_from_fields
GoToPercent.serialize = utils.serialize_from_fields
GoToPercent.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GoToPercent
function GoToPercent.deserialize(buf)
  local out = {}
  for _, v in ipairs(GoToPercent.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GoToPercent})
  out:set_field_names()
  return out
end

function GoToPercent:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param percent_open Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GoToPercent.build_test_rx(device, percent_open)
  local out = {}
  local args = {percent_open}
  for i,v in ipairs(GoToPercent.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GoToPercent})
  out:set_field_names()
  return GoToPercent._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the GoToPercent command
---
--- @param self GoToPercent the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param percent_open Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function GoToPercent:init(device, percent_open)
  local out = {}
  local args = {percent_open}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GoToPercent,
    __tostring = GoToPercent.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function GoToPercent:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GoToPercent, {__call = GoToPercent.init})

return GoToPercent
