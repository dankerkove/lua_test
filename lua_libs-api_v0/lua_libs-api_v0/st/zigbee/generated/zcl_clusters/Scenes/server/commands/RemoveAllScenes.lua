local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type SGroupId
--- @alias SGroupIdType
local SGroupIdType = require "st.zigbee.generated.zcl_clusters.Scenes.types.SGroupId"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Scenes command RemoveAllScenes
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Scenes.RemoveAllScenes
--- @alias RemoveAllScenes
---
--- @field public ID number 0x03 the ID of this command
--- @field public NAME string "RemoveAllScenes" the name of this command
--- @field public group_id st.zigbee.zcl.clusters.Scenes.types.SGroupId
local RemoveAllScenes = {}
RemoveAllScenes.NAME = "RemoveAllScenes"
RemoveAllScenes.ID = 0x03
RemoveAllScenes.args_def = {
  {
    name = "group_id",
    optional = false,
    data_type = SGroupIdType,
    is_complex = false,
    default = 0x0000,
  },
}

function RemoveAllScenes:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

RemoveAllScenes.get_length = utils.length_from_fields
RemoveAllScenes.serialize = utils.serialize_from_fields
RemoveAllScenes.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return RemoveAllScenes
function RemoveAllScenes.deserialize(buf)
  local out = {}
  for _, v in ipairs(RemoveAllScenes.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = RemoveAllScenes})
  out:set_field_names()
  return out
end

function RemoveAllScenes:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param group_id st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function RemoveAllScenes.build_test_rx(device, group_id)
  local out = {}
  local args = {group_id}
  for i,v in ipairs(RemoveAllScenes.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = RemoveAllScenes})
  out:set_field_names()
  return RemoveAllScenes._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the RemoveAllScenes command
---
--- @param self RemoveAllScenes the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param group_id st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @return ZigbeeMessageTx the full command addressed to the device
function RemoveAllScenes:init(device, group_id)
  local out = {}
  local args = {group_id}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = RemoveAllScenes,
    __tostring = RemoveAllScenes.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function RemoveAllScenes:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(RemoveAllScenes, {__call = RemoveAllScenes.init})

return RemoveAllScenes
