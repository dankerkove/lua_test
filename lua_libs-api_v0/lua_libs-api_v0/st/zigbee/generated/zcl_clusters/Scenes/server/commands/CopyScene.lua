local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type Mode
--- @alias ModeType
local ModeType = require "st.zigbee.generated.zcl_clusters.Scenes.types.Mode"
--- @type SGroupId
--- @alias SGroupIdType
local SGroupIdType = require "st.zigbee.generated.zcl_clusters.Scenes.types.SGroupId"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Scenes command CopyScene
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Scenes.CopyScene
--- @alias CopyScene
---
--- @field public ID number 0x42 the ID of this command
--- @field public NAME string "CopyScene" the name of this command
--- @field public mode st.zigbee.zcl.clusters.Scenes.Mode
--- @field public group_identifier_from st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @field public scene_identifier_from Uint8
--- @field public group_identifier_to st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @field public scene_identifier_to Uint8
local CopyScene = {}
CopyScene.NAME = "CopyScene"
CopyScene.ID = 0x42
CopyScene.args_def = {
  {
    name = "mode",
    optional = false,
    data_type = ModeType,
    is_complex = false,
  },
  {
    name = "group_identifier_from",
    optional = false,
    data_type = SGroupIdType,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "scene_identifier_from",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "group_identifier_to",
    optional = false,
    data_type = SGroupIdType,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "scene_identifier_to",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function CopyScene:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

CopyScene.get_length = utils.length_from_fields
CopyScene.serialize = utils.serialize_from_fields
CopyScene.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return CopyScene
function CopyScene.deserialize(buf)
  local out = {}
  for _, v in ipairs(CopyScene.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = CopyScene})
  out:set_field_names()
  return out
end

function CopyScene:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param mode st.zigbee.zcl.clusters.Scenes.Mode
--- @param group_identifier_from st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @param scene_identifier_from Uint8
--- @param group_identifier_to st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @param scene_identifier_to Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function CopyScene.build_test_rx(device, mode, group_identifier_from, scene_identifier_from, group_identifier_to, scene_identifier_to)
  local out = {}
  local args = {mode, group_identifier_from, scene_identifier_from, group_identifier_to, scene_identifier_to}
  for i,v in ipairs(CopyScene.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = CopyScene})
  out:set_field_names()
  return CopyScene._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the CopyScene command
---
--- @param self CopyScene the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param mode st.zigbee.zcl.clusters.Scenes.Mode
--- @param group_identifier_from st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @param scene_identifier_from Uint8
--- @param group_identifier_to st.zigbee.zcl.clusters.Scenes.types.SGroupId
--- @param scene_identifier_to Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function CopyScene:init(device, mode, group_identifier_from, scene_identifier_from, group_identifier_to, scene_identifier_to)
  local out = {}
  local args = {mode, group_identifier_from, scene_identifier_from, group_identifier_to, scene_identifier_to}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = CopyScene,
    __tostring = CopyScene.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function CopyScene:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(CopyScene, {__call = CopyScene.init})

return CopyScene
