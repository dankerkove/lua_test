local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type CcColorOptions
--- @alias CcColorOptionsType
local CcColorOptionsType = require "st.zigbee.generated.zcl_clusters.ColorControl.types.CcColorOptions"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- ColorControl command MoveColor
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.ColorControl.MoveColor
--- @alias MoveColor
---
--- @field public ID number 0x08 the ID of this command
--- @field public NAME string "MoveColor" the name of this command
--- @field public rate_x Int16
--- @field public rate_y Int16
--- @field public options_mask st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
--- @field public options_override st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
local MoveColor = {}
MoveColor.NAME = "MoveColor"
MoveColor.ID = 0x08
MoveColor.args_def = {
  {
    name = "rate_x",
    optional = false,
    data_type = data_types.Int16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "rate_y",
    optional = false,
    data_type = data_types.Int16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "options_mask",
    optional = false,
    data_type = CcColorOptionsType,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "options_override",
    optional = false,
    data_type = CcColorOptionsType,
    is_complex = false,
    default = 0x00,
  },
}

function MoveColor:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

MoveColor.get_length = utils.length_from_fields
MoveColor.serialize = utils.serialize_from_fields
MoveColor.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return MoveColor
function MoveColor.deserialize(buf)
  local out = {}
  for _, v in ipairs(MoveColor.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = MoveColor})
  out:set_field_names()
  return out
end

function MoveColor:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param rate_x Int16
--- @param rate_y Int16
--- @param options_mask st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
--- @param options_override st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function MoveColor.build_test_rx(device, rate_x, rate_y, options_mask, options_override)
  local out = {}
  local args = {rate_x, rate_y, options_mask, options_override}
  for i,v in ipairs(MoveColor.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = MoveColor})
  out:set_field_names()
  return MoveColor._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the MoveColor command
---
--- @param self MoveColor the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param rate_x Int16
--- @param rate_y Int16
--- @param options_mask st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
--- @param options_override st.zigbee.zcl.clusters.ColorControl.types.CcColorOptions
--- @return ZigbeeMessageTx the full command addressed to the device
function MoveColor:init(device, rate_x, rate_y, options_mask, options_override)
  local out = {}
  local args = {rate_x, rate_y, options_mask, options_override}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = MoveColor,
    __tostring = MoveColor.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function MoveColor:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(MoveColor, {__call = MoveColor.init})

return MoveColor
