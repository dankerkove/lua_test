local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Alarms command ResetAlarm
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Alarms.ResetAlarm
--- @alias ResetAlarm
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "ResetAlarm" the name of this command
--- @field public alarm_code Enum8
--- @field public cluster_identifier ClusterId
local ResetAlarm = {}
ResetAlarm.NAME = "ResetAlarm"
ResetAlarm.ID = 0x00
ResetAlarm.args_def = {
  {
    name = "alarm_code",
    optional = false,
    data_type = data_types.Enum8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "cluster_identifier",
    optional = false,
    data_type = data_types.ClusterId,
    is_complex = false,
  },
}

function ResetAlarm:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

ResetAlarm.get_length = utils.length_from_fields
ResetAlarm.serialize = utils.serialize_from_fields
ResetAlarm.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return ResetAlarm
function ResetAlarm.deserialize(buf)
  local out = {}
  for _, v in ipairs(ResetAlarm.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = ResetAlarm})
  out:set_field_names()
  return out
end

function ResetAlarm:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param alarm_code Enum8
--- @param cluster_identifier ClusterId
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function ResetAlarm.build_test_rx(device, alarm_code, cluster_identifier)
  local out = {}
  local args = {alarm_code, cluster_identifier}
  for i,v in ipairs(ResetAlarm.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = ResetAlarm})
  out:set_field_names()
  return ResetAlarm._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the ResetAlarm command
---
--- @param self ResetAlarm the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param alarm_code Enum8
--- @param cluster_identifier ClusterId
--- @return ZigbeeMessageTx the full command addressed to the device
function ResetAlarm:init(device, alarm_code, cluster_identifier)
  local out = {}
  local args = {alarm_code, cluster_identifier}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = ResetAlarm,
    __tostring = ResetAlarm.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function ResetAlarm:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(ResetAlarm, {__call = ResetAlarm.init})

return ResetAlarm
