local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Alarms command Alarm
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Alarms.Alarm
--- @alias Alarm
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "Alarm" the name of this command
--- @field public alarm_code Enum8
--- @field public cluster_identifier ClusterId
local Alarm = {}
Alarm.NAME = "Alarm"
Alarm.ID = 0x00
Alarm.args_def = {
  {
    name = "alarm_code",
    optional = false,
    data_type = data_types.Enum8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "cluster_identifier",
    optional = false,
    data_type = data_types.ClusterId,
    is_complex = false,
  },
}

function Alarm:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

Alarm.get_length = utils.length_from_fields
Alarm.serialize = utils.serialize_from_fields
Alarm.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return Alarm
function Alarm.deserialize(buf)
  local out = {}
  for _, v in ipairs(Alarm.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = Alarm})
  out:set_field_names()
  return out
end

function Alarm:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param alarm_code Enum8
--- @param cluster_identifier ClusterId
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function Alarm.build_test_rx(device, alarm_code, cluster_identifier)
  local out = {}
  local args = {alarm_code, cluster_identifier}
  for i,v in ipairs(Alarm.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = Alarm})
  out:set_field_names()
  return Alarm._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the Alarm command
---
--- @param self Alarm the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param alarm_code Enum8
--- @param cluster_identifier ClusterId
--- @return ZigbeeMessageTx the full command addressed to the device
function Alarm:init(device, alarm_code, cluster_identifier)
  local out = {}
  local args = {alarm_code, cluster_identifier}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = Alarm,
    __tostring = Alarm.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function Alarm:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(Alarm, {__call = Alarm.init})

return Alarm
