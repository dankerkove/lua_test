local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type IaswdLevel
--- @alias IaswdLevelType
local IaswdLevelType = require "st.zigbee.generated.zcl_clusters.IASWD.types.IaswdLevel"
--- @type SirenConfiguration
--- @alias SirenConfigurationType
local SirenConfigurationType = require "st.zigbee.generated.zcl_clusters.IASWD.types.SirenConfiguration"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASWD command StartWarning
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASWD.StartWarning
--- @alias StartWarning
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "StartWarning" the name of this command
--- @field public siren_configuration st.zigbee.zcl.clusters.IASWD.SirenConfiguration
--- @field public warning_duration Uint16
--- @field public strobe_duty_cycle Uint8
--- @field public strobe_level st.zigbee.zcl.clusters.IASWD.types.IaswdLevel
local StartWarning = {}
StartWarning.NAME = "StartWarning"
StartWarning.ID = 0x00
StartWarning.args_def = {
  {
    name = "siren_configuration",
    optional = false,
    data_type = SirenConfigurationType,
    is_complex = false,
  },
  {
    name = "warning_duration",
    optional = false,
    data_type = data_types.Uint16,
    is_complex = false,
    default = 0x0000,
  },
  {
    name = "strobe_duty_cycle",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "strobe_level",
    optional = false,
    data_type = IaswdLevelType,
    is_complex = false,
    default = 0x00,
  },
}

function StartWarning:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

StartWarning.get_length = utils.length_from_fields
StartWarning.serialize = utils.serialize_from_fields
StartWarning.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return StartWarning
function StartWarning.deserialize(buf)
  local out = {}
  for _, v in ipairs(StartWarning.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = StartWarning})
  out:set_field_names()
  return out
end

function StartWarning:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param siren_configuration st.zigbee.zcl.clusters.IASWD.SirenConfiguration
--- @param warning_duration Uint16
--- @param strobe_duty_cycle Uint8
--- @param strobe_level st.zigbee.zcl.clusters.IASWD.types.IaswdLevel
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function StartWarning.build_test_rx(device, siren_configuration, warning_duration, strobe_duty_cycle, strobe_level)
  local out = {}
  local args = {siren_configuration, warning_duration, strobe_duty_cycle, strobe_level}
  for i,v in ipairs(StartWarning.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = StartWarning})
  out:set_field_names()
  return StartWarning._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the StartWarning command
---
--- @param self StartWarning the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param siren_configuration st.zigbee.zcl.clusters.IASWD.SirenConfiguration
--- @param warning_duration Uint16
--- @param strobe_duty_cycle Uint8
--- @param strobe_level st.zigbee.zcl.clusters.IASWD.types.IaswdLevel
--- @return ZigbeeMessageTx the full command addressed to the device
function StartWarning:init(device, siren_configuration, warning_duration, strobe_duty_cycle, strobe_level)
  local out = {}
  local args = {siren_configuration, warning_duration, strobe_duty_cycle, strobe_level}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = StartWarning,
    __tostring = StartWarning.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function StartWarning:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(StartWarning, {__call = StartWarning.init})

return StartWarning
