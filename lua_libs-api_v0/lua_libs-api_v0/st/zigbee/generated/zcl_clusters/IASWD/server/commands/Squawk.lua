local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type SquawkConfiguration
--- @alias SquawkConfigurationType
local SquawkConfigurationType = require "st.zigbee.generated.zcl_clusters.IASWD.types.SquawkConfiguration"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- IASWD command Squawk
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.IASWD.Squawk
--- @alias Squawk
---
--- @field public ID number 0x01 the ID of this command
--- @field public NAME string "Squawk" the name of this command
--- @field public squawk_configuration st.zigbee.zcl.clusters.IASWD.SquawkConfiguration
local Squawk = {}
Squawk.NAME = "Squawk"
Squawk.ID = 0x01
Squawk.args_def = {
  {
    name = "squawk_configuration",
    optional = false,
    data_type = SquawkConfigurationType,
    is_complex = false,
  },
}

function Squawk:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

Squawk.get_length = utils.length_from_fields
Squawk.serialize = utils.serialize_from_fields
Squawk.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return Squawk
function Squawk.deserialize(buf)
  local out = {}
  for _, v in ipairs(Squawk.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = Squawk})
  out:set_field_names()
  return out
end

function Squawk:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param squawk_configuration st.zigbee.zcl.clusters.IASWD.SquawkConfiguration
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function Squawk.build_test_rx(device, squawk_configuration)
  local out = {}
  local args = {squawk_configuration}
  for i,v in ipairs(Squawk.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = Squawk})
  out:set_field_names()
  return Squawk._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the Squawk command
---
--- @param self Squawk the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param squawk_configuration st.zigbee.zcl.clusters.IASWD.SquawkConfiguration
--- @return ZigbeeMessageTx the full command addressed to the device
function Squawk:init(device, squawk_configuration)
  local out = {}
  local args = {squawk_configuration}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = Squawk,
    __tostring = Squawk.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function Squawk:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(Squawk, {__call = Squawk.init})

return Squawk
