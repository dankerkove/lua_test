local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type Options
--- @alias OptionsType
local OptionsType = require "st.zigbee.generated.zcl_clusters.Commissioning.types.Options"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Commissioning command ResetStartupParameters
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Commissioning.ResetStartupParameters
--- @alias ResetStartupParameters
---
--- @field public ID number 0x03 the ID of this command
--- @field public NAME string "ResetStartupParameters" the name of this command
--- @field public options st.zigbee.zcl.clusters.Commissioning.Options
--- @field public index Uint8
local ResetStartupParameters = {}
ResetStartupParameters.NAME = "ResetStartupParameters"
ResetStartupParameters.ID = 0x03
ResetStartupParameters.args_def = {
  {
    name = "options",
    optional = false,
    data_type = OptionsType,
    is_complex = false,
  },
  {
    name = "index",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function ResetStartupParameters:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

ResetStartupParameters.get_length = utils.length_from_fields
ResetStartupParameters.serialize = utils.serialize_from_fields
ResetStartupParameters.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return ResetStartupParameters
function ResetStartupParameters.deserialize(buf)
  local out = {}
  for _, v in ipairs(ResetStartupParameters.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = ResetStartupParameters})
  out:set_field_names()
  return out
end

function ResetStartupParameters:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param options st.zigbee.zcl.clusters.Commissioning.Options
--- @param index Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function ResetStartupParameters.build_test_rx(device, options, index)
  local out = {}
  local args = {options, index}
  for i,v in ipairs(ResetStartupParameters.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = ResetStartupParameters})
  out:set_field_names()
  return ResetStartupParameters._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the ResetStartupParameters command
---
--- @param self ResetStartupParameters the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param options st.zigbee.zcl.clusters.Commissioning.Options
--- @param index Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function ResetStartupParameters:init(device, options, index)
  local out = {}
  local args = {options, index}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = ResetStartupParameters,
    __tostring = ResetStartupParameters.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function ResetStartupParameters:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(ResetStartupParameters, {__call = ResetStartupParameters.init})

return ResetStartupParameters
