local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type Options
--- @alias OptionsType
local OptionsType = require "st.zigbee.generated.zcl_clusters.Commissioning.types.Options"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Commissioning command RestartDevice
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Commissioning.RestartDevice
--- @alias RestartDevice
---
--- @field public ID number 0x00 the ID of this command
--- @field public NAME string "RestartDevice" the name of this command
--- @field public options st.zigbee.zcl.clusters.Commissioning.Options
--- @field public delay Uint8
--- @field public jitter Uint8
local RestartDevice = {}
RestartDevice.NAME = "RestartDevice"
RestartDevice.ID = 0x00
RestartDevice.args_def = {
  {
    name = "options",
    optional = false,
    data_type = OptionsType,
    is_complex = false,
  },
  {
    name = "delay",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
  {
    name = "jitter",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function RestartDevice:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

RestartDevice.get_length = utils.length_from_fields
RestartDevice.serialize = utils.serialize_from_fields
RestartDevice.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return RestartDevice
function RestartDevice.deserialize(buf)
  local out = {}
  for _, v in ipairs(RestartDevice.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = RestartDevice})
  out:set_field_names()
  return out
end

function RestartDevice:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param options st.zigbee.zcl.clusters.Commissioning.Options
--- @param delay Uint8
--- @param jitter Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function RestartDevice.build_test_rx(device, options, delay, jitter)
  local out = {}
  local args = {options, delay, jitter}
  for i,v in ipairs(RestartDevice.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = RestartDevice})
  out:set_field_names()
  return RestartDevice._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the RestartDevice command
---
--- @param self RestartDevice the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param options st.zigbee.zcl.clusters.Commissioning.Options
--- @param delay Uint8
--- @param jitter Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function RestartDevice:init(device, options, delay, jitter)
  local out = {}
  local args = {options, delay, jitter}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = RestartDevice,
    __tostring = RestartDevice.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function RestartDevice:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(RestartDevice, {__call = RestartDevice.init})

return RestartDevice
