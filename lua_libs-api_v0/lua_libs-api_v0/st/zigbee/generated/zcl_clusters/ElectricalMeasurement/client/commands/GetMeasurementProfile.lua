local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- ElectricalMeasurement command GetMeasurementProfile
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.ElectricalMeasurement.GetMeasurementProfile
--- @alias GetMeasurementProfile
---
--- @field public ID number 0x01 the ID of this command
--- @field public NAME string "GetMeasurementProfile" the name of this command
--- @field public attribute_id AttributeId
--- @field public start_time UtcTime
--- @field public number_of_intervals Uint8
local GetMeasurementProfile = {}
GetMeasurementProfile.NAME = "GetMeasurementProfile"
GetMeasurementProfile.ID = 0x01
GetMeasurementProfile.args_def = {
  {
    name = "attribute_id",
    optional = false,
    data_type = data_types.AttributeId,
    is_complex = false,
  },
  {
    name = "start_time",
    optional = false,
    data_type = data_types.UtcTime,
    is_complex = false,
  },
  {
    name = "number_of_intervals",
    optional = false,
    data_type = data_types.Uint8,
    is_complex = false,
    default = 0x00,
  },
}

function GetMeasurementProfile:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

GetMeasurementProfile.get_length = utils.length_from_fields
GetMeasurementProfile.serialize = utils.serialize_from_fields
GetMeasurementProfile.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return GetMeasurementProfile
function GetMeasurementProfile.deserialize(buf)
  local out = {}
  for _, v in ipairs(GetMeasurementProfile.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = GetMeasurementProfile})
  out:set_field_names()
  return out
end

function GetMeasurementProfile:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param attribute_id AttributeId
--- @param start_time UtcTime
--- @param number_of_intervals Uint8
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function GetMeasurementProfile.build_test_rx(device, attribute_id, start_time, number_of_intervals)
  local out = {}
  local args = {attribute_id, start_time, number_of_intervals}
  for i,v in ipairs(GetMeasurementProfile.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = GetMeasurementProfile})
  out:set_field_names()
  return GetMeasurementProfile._cluster:build_test_rx_cluster_specific_command(device, out, "client")
end

--- Initialize the GetMeasurementProfile command
---
--- @param self GetMeasurementProfile the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param attribute_id AttributeId
--- @param start_time UtcTime
--- @param number_of_intervals Uint8
--- @return ZigbeeMessageTx the full command addressed to the device
function GetMeasurementProfile:init(device, attribute_id, start_time, number_of_intervals)
  local out = {}
  local args = {attribute_id, start_time, number_of_intervals}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = GetMeasurementProfile,
    __tostring = GetMeasurementProfile.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "client")
end

function GetMeasurementProfile:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(GetMeasurementProfile, {__call = GetMeasurementProfile.init})

return GetMeasurementProfile
