local data_types = require "st.zigbee.data_types"
local utils = require "st.zigbee.utils"
--- @type EffectIdentifier
--- @alias EffectIdentifierType
local EffectIdentifierType = require "st.zigbee.generated.zcl_clusters.Identify.types.EffectIdentifier"
--- @type EffectVariant
--- @alias EffectVariantType
local EffectVariantType = require "st.zigbee.generated.zcl_clusters.Identify.types.EffectVariant"

-- DO NOT EDIT: this code is automatically generated by tools/zigbee-lib_generator/generate_clusters_from_xml.py
-- Script version: b'2cda4a8ebeb3328f48b89dd179d4e8623e7ad8f1'
-- ZCL XML version: 7.2

-----------------------------------------------------------
-- Identify command TriggerEffect
-----------------------------------------------------------

--- @class st.zigbee.zcl.clusters.Identify.TriggerEffect
--- @alias TriggerEffect
---
--- @field public ID number 0x40 the ID of this command
--- @field public NAME string "TriggerEffect" the name of this command
--- @field public effect_identifier st.zigbee.zcl.clusters.Identify.EffectIdentifier
--- @field public effect_variant st.zigbee.zcl.clusters.Identify.EffectVariant
local TriggerEffect = {}
TriggerEffect.NAME = "TriggerEffect"
TriggerEffect.ID = 0x40
TriggerEffect.args_def = {
  {
    name = "effect_identifier",
    optional = false,
    data_type = EffectIdentifierType,
    is_complex = false,
  },
  {
    name = "effect_variant",
    optional = false,
    data_type = EffectVariantType,
    is_complex = false,
  },
}

function TriggerEffect:get_fields()
  local fields = {}
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      fields[#fields + 1] = self[v.name]
    end
  end
  return fields
end

TriggerEffect.get_length = utils.length_from_fields
TriggerEffect.serialize = utils.serialize_from_fields
TriggerEffect.pretty_print = utils.print_from_fields

--- Deserialize this command
---
--- @param buf buf the bytes of the command body
--- @return TriggerEffect
function TriggerEffect.deserialize(buf)
  local out = {}
  for _, v in ipairs(TriggerEffect.args_def) do
    if not v.optional or buf:remain() > 0 then
      out[v.name] = v.data_type.deserialize(buf)
    end
  end
  setmetatable(out, {__index = TriggerEffect})
  out:set_field_names()
  return out
end

function TriggerEffect:set_field_names()
  for _, v in ipairs(self.args_def) do
    if self[v.name] ~= nil then
      self[v.name].field_name = v.name
    end
  end
end

--- Build a version of this message as if it came from the device
---
--- @param device ZigbeeDevice the device to build the message from
--- @param effect_identifier st.zigbee.zcl.clusters.Identify.EffectIdentifier
--- @param effect_variant st.zigbee.zcl.clusters.Identify.EffectVariant
--- @return ZigbeeMessageRx The full Zigbee message containing this command body
function TriggerEffect.build_test_rx(device, effect_identifier, effect_variant)
  local out = {}
  local args = {effect_identifier, effect_variant}
  for i,v in ipairs(TriggerEffect.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {__index = TriggerEffect})
  out:set_field_names()
  return TriggerEffect._cluster:build_test_rx_cluster_specific_command(device, out, "server")
end

--- Initialize the TriggerEffect command
---
--- @param self TriggerEffect the template class for this command
--- @param device ZigbeeDevice the device to build this message to
--- @param effect_identifier st.zigbee.zcl.clusters.Identify.EffectIdentifier
--- @param effect_variant st.zigbee.zcl.clusters.Identify.EffectVariant
--- @return ZigbeeMessageTx the full command addressed to the device
function TriggerEffect:init(device, effect_identifier, effect_variant)
  local out = {}
  local args = {effect_identifier, effect_variant}
  if #args > #self.args_def then
    error(self.NAME .. " received too many arguments")
  end
  for i,v in ipairs(self.args_def) do
    if v.optional and args[i] == nil then
      out[v.name] = nil
    elseif not v.optional and args[i] == nil then
      out[v.name] = data_types.validate_or_build_type(v.default, v.data_type, v.name)   
    else
      out[v.name] = data_types.validate_or_build_type(args[i], v.data_type, v.name)
    end
  end
  setmetatable(out, {
    __index = TriggerEffect,
    __tostring = TriggerEffect.pretty_print
  })
  out:set_field_names()
  return self._cluster:build_cluster_specific_command(device, out, "server")
end

function TriggerEffect:set_parent_cluster(cluster)
  self._cluster = cluster
  return self
end

setmetatable(TriggerEffect, {__call = TriggerEffect.init})

return TriggerEffect
